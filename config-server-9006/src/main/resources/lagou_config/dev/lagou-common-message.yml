smtp-mail:
    lagou:
        default-encoding: utf-8
        host: smtp.126.com
        port: 465
        test-connection: true
        protocol: smtp
        username: max_translia@126.com  #发件邮箱
        password: NXPDMMDENOZPECBI
        fromAlias: max  #邮件发送人昵称
        properties:
            mail.smtp.ssl.enable: true
            mail.smtp.auth: true
            # 其实这个类可以不指定，如果不指定的话java会调用默认的实现，也就是下面这个类
            mail.smtp.ssl.socketFactory.class: javax.net.ssl.SSLSocketFactory
            mail.debug: true